parameters:
  ids : []

steps:
  - ${{each id in parameters.ids}}:
    - scripts: |
      'echo ${{ id }}'
      curl --location --request GET https://dev.azure.com/marta-mazurkiewicz/test/_apis/pipelines/1/runs/${{ id }}/logs/ -o logs.${{ id }}
